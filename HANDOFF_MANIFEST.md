# 交付清单

## 包含

- `neuro_dataset_factory/`：核心管线和 `ops` 运维命令。
- `configs/`：公开数据集发现请求。
- `examples/`：dataset seed、paper link 和 task candidate 的脱敏示例。
- `docs/`：两条管线、团队集成和数据契约说明。
- `tests/`：无需网络、模型或真实数据的离线测试。
- `pyproject.toml`、`.env.example`、`.gitignore`。